{
  "permissions": {
    "allow": [
      "Bash(npx expo start:*)",
      "Bash(npm run:*)",
      "Bash(pkill -f \"expo start\")",
      "Bash(pkill -f \"metro\")",
      "Bash(watchman watch-del *)",
      "Bash(watchman watch *)"
    ]
  },
  "systemPrompt": "Lee .claude/standards.md antes de escribir cualquier código. Lee .claude/data-model.md antes de tocar tipos de guía, mocks o render de secciones. Es el contrato de datos; no inventes campos."
}
